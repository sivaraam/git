BUILD_OPTS='NO_GETTEXT=1 DEVELOPER=1'

add_to_bashrc() {
  BASHRC_LOCATION="$HOME/.bashrc"
  echo -e  "$1" >> $BASHRC_LOCATION
  source $BASHRC_LOCATION
}

# Move to the location where the Makefile is present
cd ..
